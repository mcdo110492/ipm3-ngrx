

export interface MainContent {
    isSidenavOpen       : boolean;
    sidenavMode         : string;
    isToolbarLoader?    : boolean;
    isLoaderSpinner?    : boolean;
    isLoginPage?        : boolean;
}